FileNet
רואי משה

ספר פרויקט - FileNet

שם הפרויקט: FileNet
שם המגיש: רואי משה
ת.ז: 218049633
בית ספר: תיכון אלון
כיתה: יב10
שם מנחים: עידן פלדברג, קובי שוצמן ועודי רז, ליאת סגב
שם חלופה: הגנת סייבר / הנדסת תוכנה
תאריך הגשה: 30/12/2025

[המלצה לתמונה: לוגו מעוצב של פרויקט FileNet בעמוד השער]
!
תוכן עניינים

1.  מבוא 3
2.  ייזום 4
3.  פירוט המערכת 6
4.  מבנה וארכיטקטורה 10
5.  פירוט החומרה והטכנולוגיה 12
6.  זרימת מידע עבור כל יכולת 14
7.  אלגוריתמים מרכזיים 20
8.  תיאור סביבת הפיתוח ובדיקות 28
9.  בניית ממשק משתמש (GUI) 32
10. תיאור מבני הנתונים 38
11. סקירת חולשות ואיומים 42
12. פרוטוקול התקשורת 45
13. סיכום ורפלקציה 55
14. ביבליוגרפיה 56
15. נספחים 57
    !
16. מבוא

בעולם הטכנולוגי של ימינו, ניהול מידע ושיתוף קבצים הם חלק בלתי נפרד מכל עבודה, בין אם מדובר בסטודנטים, מפתחי תוכנה או ארגונים עסקיים. היכולת לגשת לקבצים, לערוך אותם במשותף ולשמור על גרסאות קודמות היא חיונית.
עם זאת, רוב הפתרונות הקיימים כיום בשוק (כגון Google Drive, Dropbox, GitHub) הם פתרונות ענן המנוהלים על ידי חברות ענק. המשמעות היא שהמשתמש נדרש להפקיד את המידע הרגיש שלו בידי צד שלישי, ולסמוך על נהלי האבטחה והפרטיות של אותה חברה.

פרויקט FileNet נולד מתוך הצורך לספק אלטרנטיבה: מערכת לניהול גרסאות ושיתוף קבצים המאפשרת למשתמש שליטה מלאה בשרת ובמידע, תוך דגש על אבטחת מידע קפדנית והצפנה מקצה-לקצה. המערכת משלבת את הפשטות של שרת קבצים מסורתי עם התחכום של מערכת ניהול גרסאות (Version Control), כל זאת בממשק גרפי מודרני ידידותי למשתמש.

[המלצה לתמונה: איור הממחיש את הבעיה - משתמש מבולבל מול שירותי ענן שונים וחוסר פרטיות]
! 2. ייזום

תיאור הבעיה

משתמשים רבים וארגונים קטנים נתקלים בדילמה כאשר עליהם לשתף קבצים רגישים או קוד מקור. שימוש בשירותי ענן ציבוריים חושף אותם לסיכוני פרטיות ולעיתים אף לריגול תעשייתי או דליפת מידע. מאידך, הקמת שרת FTP מסורתי היא מיושנת, לא מאובטחת באופן מובנה (הסיסמאות והמידע עוברים ב-Clear Text), וחסרה יכולות חיוניות כמו ניהול גרסאות היסטוריות של קבצים.

[המלצה לתמונה: דיאגרמה המשווה בין ענן ציבורי (Public Cloud) לענן פרטי (Private Cloud) כמו FileNet]
!
הפתרון המוצע: FileNet

FileNet היא מערכת שרת-לקוח (Client-Server) המאפשרת למשתמשים להקים "ענן פרטי" מאובטח.
המערכת מאפשרת:

- יצירת מאגרים (Repositories) פרטיים.
- שיתוף מאגרים עם משתמשים ספציפיים.
- העלאה והורדה של קבצים באופן מוצפן לחלוטין.
- ניהול היסטוריית גרסאות חכמה: המערכת שומרת כל שינוי שנעשה בקובץ ומאפשרת שיחזור של כל גרסה מן העבר.
- ממשק משתמש גרפי (GUI) מתקדם המייתר את הצורך בעבודה עם שורת פקודה.

[המלצה לתמונה: סקיצה ראשונית של הקונספט - שרת מרכזי המחובר באופן מאובטח למספר מחשבים]
!
למי מיועדת המערכת (קהל יעד)

1.  צוותי פיתוח קטנים: הזקוקים לפתרון פשוט לניהול קוד משותף ללא המורכבות של Git או התלות ב-GitHub.
2.  ארגונים בעלי מידע רגיש: הנדרשים לאחסן מסמכים בשרתים פנימיים (On-Premise) תחת אבטחה כבדה.
3.  משתמשים פרטיים טכנולוגיים: המעוניינים בגיבוי וניהול גרסאות למסמכים האישיים שלהם ברשת הביתית.

[המלצה לתמונה: אייקונים המייצגים את קהלי היעד (מפתחים, אנשי עסקים, משתמשים ביתיים)]
!
מטרות הפרויקט

1.  פיתוח מערכת תקשורת יציבה: מימוש פרוטוקול אפליקטיבי מעל TCP/IP התומך בריבוי משתמשים בו-זמנית.
2.  אבטחת מידע: יישום מנגנוני הצפנה מתקדמים (RSA להחלפת מפתחות, AES לתעבורה שוטפת, SHA-256 לגיבוב סיסמאות).
3.  ניהול מידע יעיל: שימוש באלגוריתמי Diff (הפרשים) לשמירת גרסאות בצורה חסכונית במקום לשכפל קבצים מלאים.
4.  חווית משתמש: יצירת ממשק משתמש (UI) מזמין, אינטואיטיבי ומעוצב (Dark Mode).

סקירת פתרונות קיימים והשוואה

| תכונה        | FTP רגיל             | Google Drive               | Git / GitHub               | FileNet (הפרויקט)                |
| ------------ | -------------------- | -------------------------- | -------------------------- | -------------------------------- |
| אבטחה        | נמוכה (טקסט גלוי)    | גבוהה (אך המידע אצל צד ג') | גבוהה                      | גבוהה (הצפנה מלאה, שליטה בשרת)   |
| ניהול גרסאות | אין                  | מוגבל (היסטוריית מסמכים)   | מצוין ומורכב               | טוב (מבוסס Diff, פשוט לשימוש)    |
| פרטיות       | תלוי בשרת            | נמוכה (החברה סורקת מידע)   | בינונית (בחשבונות חינמיים) | מקסימלית (שרת פרטי)              |
| ממשק משתמש   | בסיסי / תוכנות צד ג' | דפדפן / אפליקציה           | שורת פקודה / GUI מורכב     | אפליקציית Desktop ייעודית ופשוטה |

[המלצה לתמונה: גרף השוואתי ויזואלי המדגיש את יתרונות FileNet בתחומי האבטחה והפרטיות]
! 3. פירוט המערכת

מערכת FileNet מורכבת משני רכיבים עיקריים: השרת (Server.py) והלקוח (test.py / App).

תיאור כללי של פעולת המערכת

המשתמש מפעיל את אפליקציית הלקוח ומתחבר לכתובת ה-IP של השרת. לאחר ביצוע "לחיצת יד" קריפטוגרפית (Handshake) ליצירת ערוץ מאובטח, המשתמש יכול להירשם או להתחבר.
בתוך המערכת, המידע מאורגן ב"מאגרים" (Repositories). כל מאגר הוא כמו כונן וירטואלי שיכול להכיל קבצים ותיקיות. המשתמש יכול ליצור מאגרים חדשים, או לגשת למאגרים שמשתמשים אחרים שיתפו איתו.

[המלצה לתמונה: תרשים הקשר הכללי של המערכת (Context Diagram) המראה משתמשים ורכיבי מערכת]
!
סוגי משתמשים והרשאות

במערכת קיים סוג משתמש אחד עיקרי - משתמש רשום, אך לכל משתמש יש תפקידים שונים ביחס לכל קובץ/מאגר:

1.  בעלים (Owner): המשתמש שיצר את המאגר. יש לו שליטה מלאה: הוא יכול להוסיף/למחוק קבצים ולשתף את המאגר עם אחרים.
2.  מורשה גישה (Collaborator): משתמש שהבעלים נתן לו הרשאה למאגר ספציפי. הוא יכול לצפות בקבצים, להוריד אותם ולהעלות שינויים, אך לא למחוק את המאגר כולו.

[המלצה לתמונה: תרשים Use Case המתאר את הפעולות המותרות לכל סוג משתמש (Owner vs Collaborator)]
!
יכולות צד שרת (Server Capabilities)

השרת הוא "המוח" של המערכת ואחראי על:

- ניהול תעבורה המקבילית: שימוש בספריית select ובמבנה Non-blocking Sockets כדי לשרת מספר לקוחות בו-זמנית ללא עיכובים.
- אימות והרשאות: בדיקת פרטי התחברות מול מסד הנתונים (UserDB) ובדיקת הרשאות גישה לכל קובץ (ReposDB).
- אחסון חכם (Smart Storage): השרת לא סתם שומר קבצים (כמו בתיקייה רגילה), אלא משתמש במחלקה SaveHandler המנהלת "היסטוריה" (Abyss). עבור קבצי טקסט, השרת מחשב את ההבדלים (Diff) בין הגרסה החדשה לקודמת ושומר רק את השינוי כדי לחסוך באחסון.
- לוגיקה עסקית: ביצוע פקודות כמו יצירת משתמש, יצירת מאגר, חיפוש קבצים ועוד.

[המלצה לתמונה: דיאגרמת רכיבים (Component Diagram) של השרת: DBHandler, Network Manager, File System]
!
יכולות צד לקוח (Client Capabilities)

הלקוח מספק למשתמש את הכלים לעבוד מול השרת בנוחות:

- הצפנה בצד הלקוח: כל המידע (סיסמאות, תוכן קבצים) מוצפן לפני שהוא יוצא ממחשב הלקוח.
- ממשק Explorer: הצגה ויזואלית של תיקיות וקבצים עם אייקונים, בדומה ל-Windows Explorer.
- ניהול גרסאות ויזואלי: אפשרות לראות רשימה של כל הגרסאות ההיסטוריות של קובץ ולבחור איזו גרסה להוריד.
- העלאה והורדה: מנגנון להעברת קבצים בינאריים וטקסטואליים בכל גודל (עד המגבלה המוגדרת), כולל תמיכה ב-Buffering.

[המלצה לתמונה: צילום מסך של הממשק הראשי של הלקוח (Explorer View)]
! 4. מבנה וארכיטקטורה

ארכיטקטורת המערכת היא קלאסית של Client-Server (שרת-לקוח) עם מסד נתונים מרכזי וניהול קבצים מבוזר לוגית.

דיאגרמת שכבות (Layers)

1.  שכבת התצוגה (Presentation Layer - Client GUI):

    - בנויה באמצעות ספריית CustomTkinter. אחראית על קלט מהמשתמש (לחיצות כפתור, הקלדת טקסט) והצגת מידע (רשימת קבצים, הודעות שגיאה).
    - דוגמאות לרכיבים: LoginDialog, ExplorerView, SideBar.

2.  שכבת הלוגיקה והתקשורת (Application Logic & Network):

    - בצד הלקוח (SocketBackend): אחראית על בניית הפרוטוקול, הצפנת ההודעות ושליחתן לשרת.
    - בצד השרת (Server.py): מקבלת את ההודעות, מפענחת אותן, ומנתבת אותן לפונקציה המתאימה (למשל handle_login, handle_put).

3.  שכבת הנתונים (Data Access Layer - DAL):

    - מורכבת ממחלקות עזר לטיפול במסדי הנתונים: UserHandler, DBHandler, SaveHandler. שכבה זו מונעת גישה ישירה ל-SQL מהקוד הראשי ושומרת על אבטחה.

4.  שכבת האחסון (Storage Layer):

    - SQLite Databases: קבצי .db השומרים מטא-דאטה (UserDB.db, ReposDB.db, SaveDB.db).
    - File System: מערכת הקבצים של מערכת ההפעלה, בה נשמרים תוצרי הקבצים בתיקיית Abyss (היסטוריה) ו-ftp_root.

5.  פירוט החומרה והטכנולוגיה

[המלצה לתמונה: דיאגרמת ארכיטקטורת שכבות (Layered Architecture Diagram) המפרידה בין GUI, Logic, ו-Data]
!
חומרה נדרשת

המערכת תוכננה להיות קלת-משקל ולרוץ על חומרה סטנדרטית (Commodity Hardware).

- שרת: מחשב PC או שרת וירטואלי (VPS) עם מעבד סטנדרטי (i3 ומעלה), זיכרון RAM של 4GB לפחות (תלוי במספר המשתמשים), ושטח דיסק לאחסון הקבצים.
- לקוח: כל מחשב שמריץ מערכת הפעלה Windows (או Linux/Mac עם התאמות קלות) ותומך ב-Python.

טכנולוגיות וכלים

1.  Python 3.10+: שפת התכנות הראשית. נבחרה בזכות קריאות הקוד, וספריות הרשת וההצפנה החזקות שלה.
2.  Socket Programming: שימוש בספריית socket המובנית של Python למימוש תקשורת TCP ברמה נמוכה (Low-level), מה שמאפשר שליטה מלאה על הפרוטוקול.
3.  SQLite3: מסד נתונים רלציוני (SQL) קל ומהיר שלא דורש התקנת שרת נפרד. מצוין לפרויקטים בסדר גודל בינוני.
4.  CustomTkinter: ספריית מעטפת ל-Tkinter (ספריית ה-GUI המובנית של פייתון) המאפשרת עיצובים מודרניים, פינות עגולות, ותמיכה מובנית ב-Dark Mode.
5.  Cryptography Library: ספריית התקן בתעשייה ליישום אלגוריתמים קריפטוגרפיים (RSA, AES, Checksums).
6.  Win32 API (pywin32): שימוש בפונקציות מערכת של Windows (win32file) לכתיבת קבצים מתקדמת ונעילת קבצים בעת הצורך, ולטיפול בנתיבים ארוכים.

[המלצה לתמונה: קולאז' של לוגואים של הטכנולוגיות בשימוש (Python, SQLite, CustomTkinter)]
! 7. זרימת מידע עבור כל יכולת (Data Flow)

בפרק זה נתאר בצורה מפורטת מה קורה "מאחורי הקלעים" בעת ביצוע פעולות מרכזיות.

א. תהליך ההתחברות (Handshake & Key Exchange)

תהליך זה הוא קריטי לאבטחת המערכת וקורה מיד עם פתיחת האפליקציה, עוד לפני שהמשתמש רואה את מסך ההתחברות.

1.  חיבור ראשוני: הלקוח יוצר Socket ומתחבר לפורט 2122 בשרת.
2.  Banner: השרת שולח הודעת טקסט פשוטה "220 Welcome" כדי לאשר מוכנות.
3.  יצירת מפתח מושב (Session Key Generation): הלקוח מייצר בצורה רנדומלית מפתח AES באורך 32 בתים (256 ביט) ו-IV (וקטור אתחול) באורך 16 בתים.
4.  הצפנה א-סימטרית: הלקוח קורא את המפתח הציבורי (public_key.pem) של השרת (שהופץ מראש ללקוחות). הוא מצפין את מפתח ה-AES וה-IV שיצר בעזרת ה-RSA Public Key.
5.  שליחת המפתח: הלקוח שולח את הבלוק המוצפן לשרת.
6.  פענוח: השרת מקבל את הבלוק, ומשתמש במפתח הפרטי הסודי שלו (private_key.pem) כדי לפענח ולחלץ את מפתח ה-AES וה-IV.
7.  סנכרון: כעת לשני הצדדים יש מפתח הצפנה זהה (סימטרי). כל הודעה מכאן ואילך תוצפן ב-AES.

[המלצה לתמונה: דיאגרמת רצף (Sequence Diagram) של תהליך לחיצת היד והחלפת המפתחות]
!
ב. תהליך הרשמה (Register)

1.  המשתמש ממלא שם וסיסמה בטופס ולוחץ "הרשמה".
2.  הלקוח מבצע Hashing לסיסמה (SHA-256) מקומית כדי שהסיסמה האמיתית לא תישמר בזיכרון.
3.  הלקוח בונה פקודה: REGISTER <username>\_<password_hash>.
4.  הפקודה מוצפנת ב-AES ונשלחת לשרת.
5.  השרת מפענח את הפקודה ומעביר אותה ל-handle_register.
6.  השרת בודק ב-UserDB האם המשתמש קיים.
    - אם קיים: מחזיר קוד שגיאה 402.
    - אם לא קיים: מוסיף שורה לטבלת users ושולח 201 REGISTER SUCCESS.

[המלצה לתמונה: תרשים זרימה (Flowchart) של תהליך ההרשמה ואימות הנתונים]
!
ג. העלאת קובץ (Upload / PUT)

זהו תהליך מורכב הכולל העברת מידע בנפח גדול וניהול גרסאות.

1.  ייזום: הלקוח שולח פקודה PUT <path/to/file.txt>.
2.  אישור: השרת בודק הרשאות כתיבה למאגר ומחזיר 200 OK.
3.  הזרמת מידע: הלקוח קורא את הקובץ המקומי, מצפין אותו ב-AES, ושולח אותו כרצף של בתים. בסוף הקובץ נשלח הסימון המוסכם <EOF>.
4.  עיבוד בשרת: השרת אוגר את המידע בחוצץ (Buffer).
5.  בדיקת גרסאות (Server Logic):
    - השרת קורא למחלקה SaveHandler.
    - נבדק אם הקובץ כבר קיים במערכת. אם לא, הוא נשמר כגרסה 1.
    - אם הקובץ קיים והוא קובץ טקסט, השרת טוען את הגרסה הקיימת, משווה לחדשה, ומייצר Patch (רשימת השינויים בלבד).
    - השינוי (או הקובץ המלא) מתווסף לקובץ ההיסטוריה בתיקיית Abyss.
6.  סיום: השרת מעדכן את מסד הנתונים (SaveDB) ומחזיר 200 File uploaded successfully.

[המלצה לתמונה: דיאגרמה הממחישה את תהליך העלאת הקובץ והטיפול בגרסאות בצד השרת]
!
ד. סייר קבצים (List Files)

1.  הלקוח בוחר מאגר ברשימה. נשלחת פקודה LIST <repo_name>.
2.  השרת מבצע שאילתה ל-SaveDB ושולף את כל הקבצים ששייכים למאגר הזה (על בסיס ה-prefix של הנתיב).
3.  השרת מחזיר רשימה טקסטואלית של שמות קבצים.
4.  הלקוח מפרסר (Parsing) את הרשימה, מזהה מה קובץ ומה תיקייה, ומעדכן את התצוגה הגרפית (ExplorerView).

[המלצה לתמונה: צילום מסך של תצוגת הקבצים באפליקציה לאחר קבלת תשובה מהשרת]
!
אלגוריתמים מרכזיים

1.  אלגוריתם הצפנה (End-to-End Encryption flow)
    המערכת משתמשת בתבנית "מעטפה דיגיטלית".

- RSA (Rivest–Shamir–Adleman): משמש אך ורק לשלב החלפת המפתחות. RSA הוא אלגוריתם כבד חישובית, לכן לא יעיל להצפין איתו קבצים שלמים. אנו משתמשים במפתחות בגודל 2048 ביט עם ריפוד (Padding) מסוג OAEP המבטיח אקראיות ומונע התקפות מסוימות.
- AES (Advanced Encryption Standard): משמש להצפנת המידע השוטף. אנו משתמשים במצב הפעלה CBC (Cipher Block Chaining). במצב זה, כל בלוק מוצפן תלוי בבלוק הקודם, מה שמחזק את האבטחה. נדרש וקטור אתחול (IV) ייחודי לכל התחברות כדי שגם אם נשלח אותו מידע פעמיים, התוצאה המוצפנת תהיה שונה לחלוטין.

[המלצה לתמונה: איור המסביר את מושג "המעטפה הדיגיטלית" (שילוב RSA ו-AES)]
! 2. אלגוריתם ניהול גרסאות (Delta Encoding / Diff)

זהו הלב של מערכת ניהול הקבצים. במקום לשמור עותק מלא של קובץ כל פעם שהוא משתנה (דבר שינפח את הדיסק במהירות), המערכת פועלת כך:
שלבים:

1.  זיהוי: כאשר מתקבל קובץ חדש, המערכת מנסה לקרוא אותו כטקסט (utf-8). אם נכשל (מכיל תווים לא חוקיים או NULL bytes), הוא מסומן כבינארי. קבצים בינאריים נשמרים תמיד במלואם.
2.  השוואה: אם הקובץ הוא טקסט וקיימת גרסה קודמת, המערכת משתמשת בספריית difflib. היא משווה שורה-מול-שורה ומייצרת פלט בפורמט Unified Diff.

    - דוגמה לפלט:
      --- old_version
      +++ new_version
      @@ -1,2 +1,2 @@
      Hello World
      -This line is removed
      +This line is added

3.  אחסון: השרת כותב את השינוי (ה-Patch) לקובץ ההיסטוריה הפיזי (Abyss/<file_hash>) ומוסיף מפריד ייחודי: --- FNSepV<version_num> ---.

[המלצה לתמונה: דיאגרמה המראה את מבנה קובץ ההיסטוריה המכיל גרסאות מלאות וטלאים (Patches)]
! 4. אלגוריתם שחזור גרסה (Reconstruction)

כאשר משתמש מבקש להוריד קובץ בגרסה 3:

1.  השרת פותח את קובץ ההיסטוריה.
2.  הוא קורא את גרסה 1 (שהיא תמיד מלאה).
3.  הוא מחפש את גרסה 2. אם היא Patch, הוא מפעיל פונקציית apply_patch על התוכן הנוכחי.
4.  הוא מחפש את גרסה 3 ומחיל גם אותה.
5.  התוצאה הסופית נשלחת ללקוח. גישה זו חוסכת מקום בדיסק אך דורשת מעט יותר כוח עיבוד בעת ההורדה.

6.  אלגוריתם חיפוש רקורסיבי (Search)

חיפוש קבצים במבנה שטוח (Flat structure) כמו ב-Abyss לכאורה קל, אבל המשתמש רואה היררכיית תיקיות.
האלגוריתם מקבל מחרוזת חיפוש. הוא מבצע שאילתת SQL LIKE %query% על טבלת הקבצים. לאחר מכן, הוא מסנן את התוצאות ברמת הקוד כדי לוודא למשתמש יש הרשאת גישה לקובץ הספציפי שנמצא. התוצאות מוחזרות כנתיבים יחסיים (למשל MyRepo/Docs/Assignment.pdf).

[המלצה לתמונה: תרשים הממחיש את השחזור הרציף (v1 + change1 + change2 = v3)]
! 8. תיאור סביבת הפיתוח ובדיקות

כלי פיתוח:

- VS Code (Visual Studio Code): סביבת הפיתוח הראשית. שימשה לכתיבת הקוד, הרצת השרת והלקוח במקביל (באמצעות טרמינלים נפרדים), וניהול קבצי הפרויקט.
- DB Browser for SQLite: כלי גרפי שאיפשר לי לצפות בנתונים שנשמרים במסד הנתונים בזמן אמת, לוודא שטבלאות נוצרות נכון, ולדבג בעיות של שמירת מידע.
- Git: לניהול גרסאות של קוד הפרויקט עצמו (Source Control).

[המלצה לתמונה: צילום מסך של סביבת העבודה ב-VS Code עם קבצי הפרויקט פתוחים]
!

הממשק הוא הפנים של האפליקציה. הושקעה מחשבה רבה ביצירת חוויה אסתטית ונוחה.

עקרונות עיצוב

- Dark Theme: בחירה בפלטת צבעים כהה ("Slate") המקלה על העיניים ומותאמת למפתחים.
  - רקע ראשי: #0f172a (כחול כהה עמוק)
  - פאנלים: #1e293b (אפור-כחול)
  - הדגשות: #3b82f6 (כחול בהיר)
- רספונסיביות: השימוש ב-Grid Layout ו-Pack של Tkinter מאפשר לחלון להשתנות בגודלו תוך שמירה על פרופורציות.

[המלצה לתמונה: פלטת הצבעים של המערכת בפורמט ויזואלי]
!
רכיבי הממשק

1.  Login Dialog: חלון צף (Modal) המופיע במרכז המסך עם הצללה לרקע. כולל ולידציה מיידית (למשל, אורך שם משתמש).
2.  SideBar: תפריט צד קבוע המכיל את רשימת המאגרים (Repositories) של המשתמש. לחיצה על מאגר טוענת את תוכנו בחלון המרכזי. כולל כפתור "+" ליצירת מאגר חדש.
3.  Explorer View (Main Area): האזור המרכזי.

    - מציג פירורי לחם (Breadcrumbs): Repo1 > FolderA > FolderB המאפשרים ניווט מהיר למעלה.
    - רשימת פריטים: כל קובץ/תיקייה מוצגים כשורה עם אייקון מתאים (תיקייה צהובה, קובץ לבן) ושם הפריט.
    - תפריט הקשר (לא ממומש, אך מתוכנן): לחיצה ימנית להורדת גרסאות קודמות.

[המלצה לתמונה: צילום מסך של חלון ההתחברות (Login) לצד המסך הראשי עם הסברים (חיצים וכדומה)]
! 4. תיאור מבני הנתונים (Data Structures)

מסדי נתונים (SQLite Schemas)

1. UserDB.db (ניהול משתמשים)

טבלה: users
שם עמודה | סוג | תיאור
--- | --- | ---
username | TEXT (PK) | שם המשתמש (ייחודי)
hashed_password | TEXT | גיבוב הסיסמה (SHA-256)

2. ReposDB.db (ניהול הרשאות)

טבלה: files (למרות השם, משמשת לניהול מאגרים)
שם עמודה | סוג | תיאור
--- | --- | ---
id | INTEGER (PK) | מזהה ייחודי למאגר
fileName | TEXT | שם המאגר (למשל "MyProject")
ownerHash | TEXT | שם המשתמש שהוא הבעלים

טבלה: file_access (שיתוף)
שם עמודה | סוג | תיאור
--- | --- | ---
fileId | INTEGER (FK) | מזהה המאגר
accessUser | TEXT | שם משתמש שקיבל הרשאה

3. SaveDB.db (ניהול קבצים וגרסאות)

טבלה: Saves
שם עמודה | סוג | תיאור
--- | --- | ---
id | TEXT (PK) | גיבוב SHA-256 של הנתיב המלא של הקובץ
fileLoc | TEXT | הנתיב הלוגי (למשל Repo/docs/file.txt)
version | INTEGER | מספר הגרסה האחרונה שקיימת לקובץ זה

[המלצה לתמונה: דיאגרמת ישויות-קשרים (ERD) המתארת את מסדי הנתונים והקשרים ביניהם]
!
מבני נתונים בזיכרון (Server Memory)

- client_data (Dictionary): המילון הראשי שמנהל את כל הלקוחות המחוברים.
  - מפתח: אובייקט ה-Socket של הלקוח.
  - ערך: מילון המכיל:
    - addr: כתובת IP ופורט.
    - state: מצב החיבור (aes_key, aes_iv, שם משתמש מחובר, stage - האם באמצע העלאת קובץ להמתנה לפקודה).
    - buffer: אוגר מידע נכנס עד שיש פקודה מלאה.

[המלצה לתמונה: איור המתאר את מבנה הזיכרון (Heap) של השרת וכיצד הוא מנהל מספר לקוחות]
! 11. סקירת חולשות ואיומים (Threat Analysis)

בניית מערכת מאובטחת דורשת הבנה של דרכי התקיפה האפשריות.

1.  האזנת סתר (Eavesdropping / Sniffing):

    - האיום: תוקף הנמצא באותה רשת (למשל Wi-Fi ציבורי) מקליט את התעבורה ומנסה לקרוא קבצים או לגנוב סיסמאות.
    - הפתרון ב-FileNet: הצפנה מלאה. הסיסמה לעולם לא נשלחת בגלוי. גם תוכן הקבצים מוצפן ב-AES. התוקף יראה רק "רעש" בינארי חסר משמעות.

2.  התקפות Replay:

    - האיום: תוקף מקליט הודעת "Login" מוצפנת ושולח אותה שוב לשרת כדי להתחבר בשם המשתמש.
    - הפתרון: שימוש ב-IV (וקטור אתחול) רנדומלי לכל Session. הודעה שהוקלטה מסשן קודם לא תהיה תקפה בגלל שהמפתחות וה-IV משתנים בכל התחברות.

[המלצה לתמונה: איור המתאר תרחיש תקיפה "Man in the Middle" ומראה כיצד ההצפנה חוסמת אותו]
! 3. מניעת שירות (DoS - Denial of Service):

    - האיום: הצפת השרת בבקשות סרק או העלאת קובץ בגודל 100GB שימלא את הזיכרון.
    - הפתרון: הוגדרה מגבלה קשיחה MAX_FILE_SIZE = 2MB. אם לקוח מנסה לשלוח יותר, השרת מנתק את ההעלאה ומחזיר שגיאה 413. בנוסף, השימוש ב-select מונע מלקוח איטי אחד לתקוע את כל השרת.

4.  SQL Injection:

    - האיום: הכנסת פקודות SQL זדוניות בשדה שם המשתמש (למשל ' OR 1=1 --).
    - הפתרון: כל הפניות למסד הנתונים נעשות באמצעות Parameterized Queries (סימני שאלה ? ב-SQLite). המנוע של SQLite דואג לנטרל כל תו מיוחד באופן אוטומטי.

5.  פרוטוקול התקשורת (Protocol Specification)

הפרוטוקול הוא השפה שבה השרת והלקוח מדברים. הוא מבוסס על הודעות טקסט (פקודות) המועברות בתוך עטיפה (Wrapper) בינארית מוצפנת.

[המלצה לתמונה: איור של מבנה המסד (DB) לעומת השאילתות המוגנות]
!
מבנה החבילה (Packet Structure)

כל הודעה ברשת בנויה כך:
[ 4 Bytes Length (Big Endian) ] [ Encrypted Payload (AES-CBC) ]

1.  Length: מספר שלם המייצג את אורך התוכן המוצפן שבא אחריו. זה מאפשר לשרת לדעת מתי נגמרת הודעה אחת ומתחילה הבאה.
2.  Payload: הפקודה עצמה, מוצפנת.

מילון הפקודות המלא

| פקודה         | ארגומנטים      | תיאור                      | דוגמה                    |
| ------------- | -------------- | -------------------------- | ------------------------ |
| LOGIN         | User_PassHash  | כניסה למערכת               | LOGIN admin_5e884...     |
| REGISTER      | User_PassHash  | רישום משתמש חדש            | REGISTER newuser_9f33... |
| LIST          | [Path]         | בקשת רשימת קבצים           | LIST MyRepo/Images       |
| GET           | Path [Version] | הורדת קובץ (אופציות לגרסה) | GET MyRepo/file.txt 2    |
| PUT           | Path           | בקשה להעלאת קובץ           | PUT MyRepo/new_doc.pdf   |
| MKDIR         | Path           | יצירת תיקייה לוגית (Touch) | MKDIR MyRepo/NewFolder   |
| CREATEREPO    | Name           | יצירת מאגר ראשי חדש        | CREATEREPO SummerProject |
| ADDUSER       | Repo_User      | שיתוף מאגר עם חבר          | ADDUSER ProjectX_dani    |
| GETREPOS      | -              | קבלת רשימת המאגרים שלי     | GETREPOS                 |
| SEARCH        | FileName       | חיפוש בכל המאגרים          | SEARCH tax_report        |
| CHANGEPASS    | Old_New        | החלפת סיסמה                | CHANGEPASS hash1_hash2   |
| DELETEACCOUNT | -              | מחיקת המשתמש לצמיתות       | DELETEACCOUNT            |
| QUIT          | -              | ניתוק מסודר                | QUIT                     |

[המלצה לתמונה: טבלה ויזואלית או דיאגרמה המרכזת את כל הפקודות והמבנה שלהן]
!
קודי תגובה (Response Codes)

השרת מחזיר תשובות בפורמט הדומה ל-HTTP:

- 2xx - הצלחה (200 OK, 201 Created).
- 4xx - שגיאת לקוח (400 Bad Request, 401 Unauthorized, 403 Forbidden, 404 Not Found).
- 5xx - שגיאת שרת (500 Server Error).

[המלצה לתמונה: איור פשוט של רמזור או סמלים המייצגים הצלחה (ירוק) ושגיאה (אדום)]
! 13. סיכום ורפלקציה

העבודה על פרויקט FileNet הייתה אתגר משמעותי ומלמד.
במהלך הפיתוח נחשפתי לעומקם של נושאים שבדרך כלל משתמשים בהם כ"קופסה שחורה", כגון פרוטוקולי תקשורת והצפנה. כתיבת מנגנון ה-Diff והלוגיקה של ניהול הגרסאות הראתה לי כמה מורכבת מערכת כמו Git מאחורי הקלעים, אך גם כמה סיפוק יש ביצירת פתרון שעובד וחוסך מקום.
התמודדתי עם אתגרים של סנכרון (Threads vs Select), ניהול מבני נתונים בינאריים, ותכנון ממשק משתמש שירגיש טבעי וזורם.
אני מאמין שהתוצר הסופי עומד ביעדים שהצבתי: מערכת עובדת, מאובטחת ושימושית.

14. ביבליוגרפיה

1.  Python Cartography Authority. "Cryptography - Fernet and Primitives". cryptography.io.
1.  Schouman, Tomer. "CustomTkinter Documentation". GitHub.
1.  Python Software Foundation. "difflib — Helpers for computing deltas".

[המלצה לתמונה: תמונת סיום, אולי צילום מסך של המערכת בפעולה מלאה או איור של "V" לסימון הצלחה]
!

1.  נספחים

(ראה קבצי קוד מצורפים: Server.py, test.py, SaveHandler.py, DBHandler.py, UserHandler.py)

[המלצה לתמונה: צילום מסך של מבנה התיקיות של הפרויקט]
